
# 📌 Function<String, Integer> 와 Integer.parseInt(String) 매칭은 어떻게 이루어지는가?

자바에서 메서드 참조는 함수형 인터페이스의 구현체가 되기 위해 **시그니처가 완전히 호환되어야** 합니다.
여기서 시그니처에는 파라미터 타입뿐 아니라 **리턴 타입까지 포함**됩니다.

---

# ✔ 시그니처 비교 표 (정확한 타입 포함)

우선, 다음 코드:

```java
Function<String, Integer> f = Integer::parseInt;
```

이 코드가 유효하려면 다음 조건이 충족돼야 합니다.

### 1) 함수형 인터페이스 시그니처

`Function<T, R>` 의 추상 메서드:

```java
R apply(T t)
```

여기에 타입을 대입하면:

| 항목 | 타입      |
| -- | ------- |
| T  | String  |
| R  | Integer |

즉,

```java
Integer apply(String t);
```

---

### 2) 참조되는 실제 메서드 시그니처

`Integer.parseInt(String s)` 의 선언은 다음과 같습니다:

```java
public static int parseInt(String s)
```

리턴 타입은 **int(기본 타입)**입니다.

---

# ✔ 둘의 시그니처는 어떻게 매칭되는가?

| 항목    | Function<String, Integer>.apply | Integer.parseInt |
| ----- | ------------------------------- | ---------------- |
| 파라미터  | String                          | String           |
| 리턴 타입 | Integer                         | int              |

여기서 핵심:

### 🔍 int → Integer 는 자동으로 호환된다

자바는 int 기본 타입을 자동으로 **오토박싱(autoboxing)**해서 Integer 객체로 변환할 수 있습니다.

따라서 다음 구문은 유효합니다:

```java
Integer result = Integer.parseInt("10"); // int → Integer 자동 변환
```

이 규칙이 있기 때문에 Function<String, Integer> 의 **R = Integer** 가
정적 메서드인 `int parseInt(String)` 과 호환됩니다.

---

# ✔ 즉, 매칭 조건은 다음과 같다

### ➤ 파라미터 타입: 정확히 일치

### ➤ 리턴 타입: int → Integer 자동 박싱 가능

→ 타입 호환 성공
→ 메서드 참조 가능
→ Function<String, Integer> 구현체 생성

---

# ✔ 컴파일러가 실제로 생성하는 형태(람다 기반)

실제로는 다음 람다로 변환된 것과 같습니다.

```java
Function<String, Integer> f = (String s) -> {
    return Integer.valueOf(Integer.parseInt(s)); 
};
```

이는 다음과 동등합니다.

```java
return Integer.parseInt(s); // 자동 박싱
```

컴파일러는 이런 변환 규칙을 내장하고 있기 때문에
**리턴 타입에 대한 호환까지 자동으로 처리**해 줍니다.

---

# ✔ 이 규칙이 깨지는 경우는?

다음은 유효하지 않습니다:

```java
Function<String, Long> f = Integer::parseInt;
```

리턴 타입 매칭:

| Function 리턴 | parseInt 리턴 |
| ----------- | ----------- |
| Long        | int         |

**int → Long 자동 변환은 불가능**합니다.
(오토박싱은 Integer로만 가능)

그래서 컴파일 오류가 발생합니다.

---

# ✔ 실무 팁: 기본 타입을 리턴하는 메서드를 참조할 때 주의할 점

메서드 참조에서 리턴 타입이 primitive(기본형)인 경우는 다음도 참고할 필요가 있습니다.

### 예시: ToIntFunction vs Function

```java
ToIntFunction<String> f1 = Integer::parseInt;   // OK, 리턴 타입 그대로 int
Function<String, Integer> f2 = Integer::parseInt; // OK, 박싱 필요
Function<String, Long> f3 = Integer::parseInt;     // ❌ 오류
```

더 효율적인 구현을 원하면 기본형 특화 함수형 인터페이스를 사용하는 것이 좋습니다.

---

# ✔ 결론: 리턴 타입 매칭은 “타입 일치 + 오토박싱 허용” 규칙으로 동작한다

요약:

1. Function<String, Integer> 는 `Integer apply(String)` 시그니처를 가진다.
2. Integer.parseInt(String) 은 `int`를 반환한다.
3. int는 자동으로 Integer로 박싱된다.
4. 따라서 시그니처가 호환된다.
5. 그 결과 `Integer::parseInt` 는 Function<String, Integer> 의 **정상적인 구현체**가 될 수 있다.

